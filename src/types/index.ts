export type TableRow = {
  fullName: string;
  company: string;
  socialMedia: string;
  description: string;
  listName: string;
};

export type List = {
  name: string;
  link: string;
  title: string;
  repoLink: string;
};
